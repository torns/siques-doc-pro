import { AutoincrementInterceptor } from './autoincrement.interceptor';

describe('AutoincrementInterceptor', () => {
  it('should be defined', () => {
    expect(new AutoincrementInterceptor()).toBeDefined();
  });
});
